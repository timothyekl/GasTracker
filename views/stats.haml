- if Purchase.all.length == 0
    = "Add a purchase to get started!"
- else
    = "Total gallons purchased: #{(Purchase.all.map &:gallons).inject(:+)}"
    %br
    = "Total miles traveled: #{(Purchase.all.map &:miles).inject(:+)}"
    %br
    = "Total cost: #{"%.2f" % ((Purchase.all.map &:cost).inject(:+))}"
    %hr
    = "Average gallons per purchase: #{(Purchase.all.map &:gallons).inject(:+) / Purchase.all.length}"
    %br
    = "Average miles per purchase: #{(Purchase.all.map &:miles).inject(:+) / Purchase.all.length}"
    %br
    = "Average cost per purchase: #{"%.2f" % ((Purchase.all.map &:cost).inject(:+) / Purchase.all.length)}"
    %br
    = "Average miles per gallon: #{(Purchase.all.map &:miles).inject(:+) / (Purchase.all.map &:gallons).inject(:+)}"
    %br
    = "Average cost per gallon: #{"%.2f" % ((Purchase.all.map &:cost).inject(:+) / (Purchase.all.map &:gallons).inject(:+))}"
